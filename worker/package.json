{
  "name": "bolt-tarot-worker",
  "version": "1.0.0",
  "private": true,
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev --port 8787",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "test:oracle-quota": "node --experimental-strip-types scripts/oracle-quota-check.ts",
    "test:member-metadata": "node scripts/member-metadata-check.mjs",
    "test:admin-members": "node --experimental-strip-types scripts/admin-members-check.ts",
    "types": "wrangler types"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260101.0",
    "typescript": "^5.5.3",
    "wrangler": "^4.105.0"
  }
}
